---
summary: "CO-OPS throttling as a typed error, residual sign clamp, hyphenated dates"
breaking: false
security: false
---

# 0.5.1 — 2026-09-22

## Added

- **`begin_date`/`end_date`** on `noaa_marine_get_tide_predictions`, `noaa_marine_get_water_level`, and `noaa_marine_get_currents` accept `YYYY-MM-DD` alongside `YYYYMMDD`, normalized per field ([#39](https://github.com/cyanheads/noaa-marine-mcp-server/issues/39))

## Fixed

- **`upstream_throttled`** — a CO-OPS HTTP 403 (per-customer throttling) now maps to a retryable `RateLimited` error instead of a bare fetch failure, on the three date-ranged tools and in the `sources_unavailable`/`source_unavailable` recovery from `noaa_marine_find_stations` and the station resource ([#37](https://github.com/cyanheads/noaa-marine-mcp-server/issues/37))
- **`residual_summary`** on `noaa_marine_get_water_level` clamps `max_surge`/`max_drawdown` at zero, so a one-sided observation window no longer reports a surge or drawdown that never occurred ([#35](https://github.com/cyanheads/noaa-marine-mcp-server/issues/35))

## Dependencies

- `@cyanheads/mcp-ts-core` ^0.13.3 → ^0.13.6
- `@biomejs/biome` ^2.5.13 → ^2.5.14
- `@types/node` ^26.5.1 → ^26.6.2
- `vitest` ^4.1.11 → ^5.0.1
